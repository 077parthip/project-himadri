# Himadri dashboard dependencies
streamlit>=1.37        # st.fragment(run_every=...) needs 1.37+
pandas>=2.0
plotly>=5.20
paho-mqtt>=2.0         # optional: only used in "Live MQTT" mode (v2 callback API)
